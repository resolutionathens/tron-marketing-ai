{
  "name": "tron-imagekit-cli",
  "version": "1.0.0",
  "private": true,
  "description": "Vendored ImageKit CDN CLI for the tron plugin. Uses the org-secret broker for auth — no ImageKit SDK needed.",
  "type": "module",
  "bin": { "imagekit": "./imagekit.mjs" },
  "engines": {
    "node": ">=18.0.0"
  }
}
