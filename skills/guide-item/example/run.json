{
  "skills": ["guide-item"],
  "mode": "judge",
  "query": "Build out the guide for MD-704 — it's a Pillar ticket for a Preventive Maintenance Strategy guide with a Confluence draft linked in the description. Get it on /resources/guides.",
  "files": [],
  "expected_behavior": [
    "Runs the marketing-pages preflight guard (content.sh check-repo) and refuses to write if the checkout is not marketing-pages",
    "Reads the Jira ticket via acli, fetches the linked Confluence draft and its images, and confirms the slug (e.g. preventive-maintenance-strategy) with the user before uploading",
    "Composes a hand-built Vue page at pages/resources/guides/<slug>.vue from the guide component palette (BaseSection, callout, alternative grids, SectionAccordion) rather than a Nuxt-Content markdown file or ::fImg/::image-text blocks",
    "Uploads body images to guides/<slug>/<name>.webp, an OG image to og/og-<slug>.webp, and a card thumbnail to guides/guide-<NN>.webp at the next free index, referencing body images by relative provider path",
    "Wires useDynamicMeta (title, keyword-rich description, route path, full OG URL), keeps TOC ids in sync with BaseSection ids, uses only tron- tokens (no arbitrary Tailwind values), and appends a new entry to the resourceGuides array in app/data/resource-guides.ts",
    "Produces copy with no em dashes (Facilitron voice), runs the verification loop (renders the route on this worktree, prose-lint, a11y-scan, em-dash grep), and cleans up /tmp and dropped-in sources"
  ]
}
